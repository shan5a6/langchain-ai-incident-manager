global:
  resolve_timeout: 5m

route:
  receiver: 'rag-service-webhook'

receivers:
  - name: 'rag-service-webhook'
    webhook_configs:
      - url: "http://rag-service:8081/alert"
        send_resolved: true
